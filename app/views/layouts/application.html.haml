!!!
%html
  %head
    %title Books please!
    %meta{:charset => "utf-8"}
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1, shrink-to-fit=no"}

    = csrf_meta_tags

    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track': 'reload'

  %body

    = render "shared/navbar"

    .container.pt-3.pb-5
      - flash.each do |key, value|
        - alert_class = key == 'error' ? 'danger' : 'success'
        #alert.alert.alert-dismissible.fade.show{class: "alert-#{alert_class}", role: :alert}
          %button(type="button" class="close" data-dismiss="alert" aria-label="Close")
            %span(aria-hidden="true") &times;
          = value
      = yield

  %footer.text-center.fixed-bottom
    %small.text-muted Books please! v#{BooksPlease::Application::VERSION}